name: CD - Deploy to Production

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.nix'
      - '.env.production'
      - '**.md'
      - '.github/**'

jobs:
  version-bump:
    runs-on: [self-hosted, windows]
    environment: production
    outputs:
      version: ${{ steps.version.outputs.version }}
      skip: ${{ steps.bump.outputs.skip }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Determine version bump
        id: bump
        shell: powershell
        run: |
          $COMMIT_MSG = git log -1 --pretty=%B
          Write-Host "Commit message: $COMMIT_MSG"

          if ($COMMIT_MSG -match '^(chore|docs|style|test|misc)') {
            "skip=true" >> $env:GITHUB_OUTPUT
            Write-Host "Skipping deployment for: $COMMIT_MSG"
            exit 0
          }

          if ($COMMIT_MSG -match 'BREAKING CHANGE') {
            "bump=major" >> $env:GITHUB_OUTPUT
          } elseif ($COMMIT_MSG -match '^feat') {
            "bump=minor" >> $env:GITHUB_OUTPUT
          } else {
            "bump=patch" >> $env:GITHUB_OUTPUT
          }

      - name: Bump version in package.json
        if: steps.bump.outputs.skip != 'true'
        id: version
        shell: powershell
        run: |
          cd frontend
          npm version ${{ steps.bump.outputs.bump }} --no-git-tag-version
          $VERSION = node -p "require('./package.json').version"
          "version=$VERSION" >> $env:GITHUB_OUTPUT
          Write-Host "Bumped to: beta-v$VERSION"

      - name: Commit version bump
        if: steps.bump.outputs.skip != 'true'
        shell: powershell
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add frontend/package.json
          git commit -m "chore: bump version to beta-v${{ steps.version.outputs.version }}"
          git push origin main

  prepare:
    needs: version-bump
    if: needs.version-bump.outputs.skip != 'true'
    runs-on: [self-hosted, windows]
    environment: production

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Restore appsettings.json
        shell: powershell
        run: |
          Copy-Item -Path "C:\Users\Ron\Desktop\secrets\appsettings.json" `
                    -Destination "${{ github.workspace }}\backend\appsettings.json" `
                    -Force
          Write-Host "appsettings.json restored"

      - name: Generate .env.prod
        shell: powershell
        run: |
          "APP_VERSION=beta-v${{ needs.version-bump.outputs.version }}" | Out-File .env.prod -Encoding utf8
          Write-Host ".env.prod generated"

  deploy:
    needs: [version-bump, prepare]
    if: needs.version-bump.outputs.skip != 'true'
    runs-on: [self-hosted, windows]
    environment: production

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Restore appsettings.json
        shell: powershell
        run: |
          Copy-Item -Path "C:\Users\Ron\Desktop\secrets\appsettings.json" `
                    -Destination "${{ github.workspace }}\backend\appsettings.json" `
                    -Force

      - name: Generate .env.prod
        shell: powershell
        run: |
          "APP_VERSION=beta-v${{ needs.version-bump.outputs.version }}" | Out-File .env.prod -Encoding utf8

      - name: Build images
        shell: powershell
        run: |
          Write-Host "Building new images..."
          docker compose --env-file .env.prod --profile prod build
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Switch containers
        shell: powershell
        run: |
          Write-Host "Switching to new containers..."
          docker compose --env-file .env.prod --profile prod up -d
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Cleanup
        shell: powershell
        run: |
          Write-Host "Cleaning up old images..."
          docker image prune -f

  notify:
    needs: [version-bump, deploy]
    if: always() && needs.version-bump.outputs.skip != 'true'
    runs-on: [self-hosted, windows]

    steps:
      - name: Notify success
        if: needs.deploy.result == 'success'
        shell: powershell
        run: Write-Host "Successfully deployed beta-v${{ needs.version-bump.outputs.version }}"

      - name: Notify failure
        if: needs.deploy.result == 'failure'
        shell: powershell
        run: Write-Host "Deployment failed for commit ${{ github.sha }}"
